<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=" ">
	<!-- 공지사항 작성 -->
	<insert id="insertNotice">
		INSERT INTO GR_NOTICE(NT_TITLE, AD_ID, NT_CON, NT_HITS)
		VALUES (#{nt_title}, #{ad_id}, #{nt_con}, 0)
	</insert>
	
	<!-- 공지사항 전체조회 -->
	<select id="selectAllNotice" resultType="NoticeDTO">
		SELECT * FROM GR_NOTICE
		ORDER BY NT_NO DESC
	</select>
	
	<!-- 공지사항 수정 -->
	<update id="updateNotice">
		UPDATE GR_NOTICE
		SET NT_TITLE = #{nt_title}, NT_CON = #{nt_con}
		WHERE NT_NO = #{nt_no}
	</update>
	
	<!-- 공지사항 삭제 -->
	<delete id="deleteNotice">
		DELETE FROM GR_NOTICE
		WHERE NT_NO = #{nt_no}
	</delete>
	
</mapper>